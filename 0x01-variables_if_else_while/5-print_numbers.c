#include <stdio.h>

/**
 * main - program entry point
 * Description - print 0 - 9 numerals
 * Return: return 0 if success
 */

int main(void)
{
	for (int i = 0; i <= 9; i++)
	{
		printf("%d", i);
	}
	printf("\n");

	return (0);
}
