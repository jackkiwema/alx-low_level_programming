#include <stdio.h>

/**
 * main - program entry point
 * Description - print numerals 0 - 9 
 * Return: return 0 if success
 */

int main(void)
{
	for (int i = 48; i <= 57; i++)
	{
		putchar(i);
	}
	putchar(10);

	return (0);
}
